node_modules/
.next/
coverage/
next-env.d.ts
# tsc --noEmit 的增量編譯快取（tsconfig 的 incremental: true）；每次執行內容都不同，
# 進版控只會製造分支間的 add/add 衝突。
tsconfig.tsbuildinfo
dev.db
dev.db-wal
dev.db-shm
.env

# Claude Code 個人工作環境：skills、agents、hooks、statusline 二進位檔皆屬本機設定，
# 不進版控。專案規範（CLAUDE.md、rules/）例外保留。
.claude/*
!.claude/CLAUDE.md
!.claude/rules/
